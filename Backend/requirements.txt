# Faust stream processing framework
faust==1.10.4
robinhood-aiokafka==1.1.6

# Data validation and models
pydantic==2.8.2

# Configuration management
python-dotenv==1.0.1

# Database drivers
asyncpg==0.29.0
neo4j==5.21.0
redis==5.0.4

# Graph algorithms (Louvain community detection)
# networkx 3.2.1 is the last release supporting Python 3.9 — do not bump without a Python upgrade
networkx==3.2.1

# Vectorized edge aggregation (directed FLOWS_TO -> undirected graph merge)
pandas==2.3.3
numpy==2.0.2

# Optional Leiden community-detection engine (LOUVAIN_ENGINE=leiden).
# PyPI package is `igraph` (NOT the deprecated `python-igraph`). Both are
# GPL-3.0 — the default engine stays networkx so this stays opt-in.
igraph==1.0.0
leidenalg==0.12.0

# Web framework (included with Faust but explicit for clarity)
aiohttp==3.10.1

# FastAPI serving layer (graph query API — app/api)
fastapi==0.115.0
pydantic-settings==2.4.0
uvicorn[standard]==0.30.6

# Logging utilities
python-json-logger==2.0.7

# Testing
pytest==8.2.2
pytest-asyncio==0.23.8
testcontainers==4.7.2
