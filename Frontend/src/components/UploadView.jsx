import { DemoNote } from './ui'

/** Placeholder until Plan C wires the dataset upload flow (live mode only). */
export default function UploadView() {
  return (
    <div className="px-8 pt-4">
      <DemoNote>Uploading your own transaction file is coming in the next step.</DemoNote>
    </div>
  )
}
