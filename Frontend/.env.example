# Base URL of the FlowGraph backend API (FastAPI graph query service).
# Copy this file to `.env` and adjust for your environment.
# Falls back to http://localhost:8000/api when unset (see src/services/api.js).
VITE_API_BASE_URL=http://localhost:8000/api
